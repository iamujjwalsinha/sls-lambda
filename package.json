{
  "name": "sls-lambda",
  "version": "1.0.0",
  "description": "To create AWS lambda through AWS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ujjwal Sinha",
  "license": "ISC",
  "devDependencies": {
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
